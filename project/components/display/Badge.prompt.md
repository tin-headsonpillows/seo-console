Category tags on blog/resource cards, and small accent labels inside product-UI fragments.

```jsx
<Badge>Product</Badge>
<Badge tone="emerald">New</Badge>
```

`neutral` (light-gray) is the default for most tags; pastel tones are the exception, not the rule.
