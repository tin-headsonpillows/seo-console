Used at the left of `TopNav` and top-left of `Footer`. No logo mark exists in the source material — see readme's ICONOGRAPHY section — so this is plain Manrope 600 type, not a substitute icon.

```jsx
<Wordmark />
<Wordmark tone="on-dark" />
```
