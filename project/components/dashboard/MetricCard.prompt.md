```jsx
<MetricCard label="Total clicks" value="489" color="var(--db-clicks)" active onClick={() => toggle("clicks")} />
```

Used in a 2/4-up grid above the performance chart — one per metric (clicks, impressions, CTR, position).
