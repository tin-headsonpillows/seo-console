Reproduces the app's exact filter set, including the two features GSC doesn't have: **People Also Ask** and **AI search prompts**.

```jsx
<FilterPopover value={filters} onChange={setFilters} dimension="query" />
```
