The individual segment/tab primitive — `NavPillGroup` composes several of these. Use standalone for a filter row that isn't wrapped in the soft-gray pill.

```jsx
<CategoryTab active>Product</CategoryTab>
<CategoryTab>Article</CategoryTab>
```
