Used in 3-up grids for abstract feature claims ("With us, SEO workflow management is easy").

```jsx
<FeatureCard icon={<Icon name="zap" size={24} />} title="Automated audits" description="Run a full technical crawl on a schedule you set." />
```

Use `ProductMockupCard` instead when the point is to show real product chrome rather than a claim.
