import * as React from "react";

/** The larger marquee card that holds the hero's product-dashboard mockup — 16px radius (the system's largest), subtle drop shadow. */
export interface HeroAppMockupCardProps {
  children?: React.ReactNode;
  style?: React.CSSProperties;
}

export declare function HeroAppMockupCard(props: HeroAppMockupCardProps): JSX.Element;
