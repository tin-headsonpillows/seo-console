Used in 4-up feature grids on secondary bands, where `FeatureCard`'s gray fill would be too heavy repeated four times.

```jsx
<FeatureIconCard icon={<Icon name="link" size={20} />} title="Backlink tracking" description="See every link gained or lost, daily." />
```
