The right-side artifact in `HeroBand`'s 7/5 split — holds a keyword grid, ranking positions, and a primary "Run audit" button.

```jsx
<HeroAppMockupCard>
  <DashboardFragment />
</HeroAppMockupCard>
```

The only card allowed the 16px `--radius-xl` — reserved for this one marquee component.
