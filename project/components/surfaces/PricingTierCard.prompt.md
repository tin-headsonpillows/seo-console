```jsx
<PricingTierCard name="Starter" price="$29" features={["1 site", "Weekly audits", "Email support"]} />
<PricingTierCard featured name="Pro" price="$79" features={["10 sites", "Daily audits", "Priority support"]} />
```

Use `featured` on exactly one card per grid (typically "Pro") — it is the system's only other dark surface besides the footer, so it must stay rare.
