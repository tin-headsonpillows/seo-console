The frame for "look at the actual product" moments — keyword grids, ranking widgets, schedule pickers, integration grids.

```jsx
<ProductMockupCard>
  <KeywordGridFragment />
</ProductMockupCard>
```

Never put an abstract icon + text claim inside this card — that's `FeatureCard`'s job. This card's contents should look like real software chrome.
